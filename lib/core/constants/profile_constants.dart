import 'api_constants.dart';

class ProfileConstants {
  static String get profile => '${ApiConstants.baseUrl}/user/profile';
  static String get changePassword => '${ApiConstants.baseUrl}/user/change-password';
}